#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    cout << (a + b > 30 && a * b > 30) << " ";
    cout << (a + (b - 10) > 30 || a * (b - 10) > 30) << " ";
    cout << (a > 30 || b > 30);
    return 0;
}
