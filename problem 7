#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double x, y;
    if(!(cin >> x >> y)) return 0;
    double t = (3 * (x * x + 3)) / (y / 4.0 + 3);
    cout << "t=" << fixed << setprecision(5) << t;
    return 0;
}

//The example given in the pdf file is wrong
