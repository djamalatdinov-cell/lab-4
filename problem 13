#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double s;
    cin >> s;
    double area = 3 * sqrt(3) * s * s / 2;
    cout << "The area of the hexagon is " << area;
    return 0;
}
